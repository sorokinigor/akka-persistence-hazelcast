akka {
  loglevel = "DEBUG"
  stdout-loglevel = "DEBUG"

  persistence {
    journal {
      plugin = "hazelcast.journal"
    }
  }
}

# Hazelcast Extenstion
hazelcast {
  config-file = "hazelcast.xml"
}
